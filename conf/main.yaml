defaults:
  - model: xgboost
  - data: base
  - optuna: base

seed: 42

n_splits: 10

voting:
  is_average_voting: true

preprocessing:
  per_time: true
  pf: true
  pca: true
  new_col: true
  diff_el: true
  song_vector: true
